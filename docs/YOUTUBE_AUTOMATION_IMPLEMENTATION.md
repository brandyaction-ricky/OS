# YouTube automation implementation contract

Status: development specification. This file describes application behavior; BRANDYACTION OS remains the source of company content rules and approvals.

## Scope and sequence

For each `content_topic`, retain the existing content ID and artifact versions. Resolve the live OS rules at execution time. Follow the canonical sequence: topic plan, selected title and thumbnail copy, narration script, media production, review, private YouTube upload. A board or shooting-plan format cannot enter the cloned-narration path without an approved full script.

The first production format is the owner's cloned voice over a clean explanatory screen with the existing BRANDYACTION character illustrations, short Korean text, and progressively drawn diagrams. The approved motion study reveals the character outline, fills the original colors, then draws a simple relationship diagram. It is a new `narrated_visual` production format, not a change to the existing shortform clip renderer. Photoreal illustrative B-roll is optional and must be justified by the script and evidence. Private upload is the only permitted visibility for automated jobs. Public release remains a distinct workflow.

The current visual contract is `brandyaction-vector-scene-v7`, chosen by the owner on 2026-09-24 over the fixed-template v6 renderer after an A/B study. Opus authors one restricted SVG drawing per spoken beat: a beat-specific metaphor (for example a battery that charges, a stopwatch and a long line, a loop with a magnifier) drawn in reveal order, with short Korean labels, one red accent, and optional channel character art referenced by catalog id. The canvas contains only content: no brand header, English footer, border, or watermark. The headline is rendered by the system above y=180 and appears at a verified spoken word after the drawing starts. `lib/youtube-scene-svg.ts` is the trust boundary shared by the server and the renderer: a whitelisted element and attribute subset, palette-only colors, class-based styles, bounded numbers, and no scripts, links, styles, foreign content or unregistered art. Character art is the owner's private channel catalog (`brandyaction-character-catalog-v2`, 60 scenes, 53 usable on white; seven black-background scenes need cut-out versions). The catalog and PNGs stay outside Git in the private `os-youtube-character` bucket (migration `20260924090000_youtube_character_assets.sql`); scene planning is unavailable until `catalog.json` is stored there. Plans record the catalog digest and are regenerated when it changes. Existing plans without the current template version are stale and must be regenerated before new work starts.

The development pilot reads four live canonical OS documents (brand context, editing, audit, production design) via server-side `YOUTUBE_SCENE_RULE_IDS`; identifiers and document contents stay out of Git. It requires explicit configuration of that mapping in the development environment. Scene previews are invalidated when the mapped document versions change. The pilot also requires distinct DEV and Production Supabase references and an exact DEV URL match; it is unavailable on Production or a main/master deployment.

| Owner | Responsibility |
| --- | --- |
| Claude Opus 5.5 | Scene direction (`claude-opus-5-5`, effort `high`); planned final multimodal review |
| Claude Sonnet 5 | Voice pronunciation review before Fish synthesis (`claude-sonnet-5`); bounded execution of a versioned brief |
| OpenAI image model | Optional generated still preview only; Claude has no image generation, so this is the remaining OpenAI dependency |
| Existing System One layer | Typed advisory judgments; currently JEV packaging shadow in DEV only |
| Fish Audio | Owner voice synthesis from the configured private voice reference |
| Media worker | Generate assets and render audio, scenes, captions and final MP4 outside Vercel request time limits |
| Existing YouTube adapter | OAuth upload and verification, extended for scoped private-only worker execution after final gate |

## Boundary with the JEV project

The existing System One project owns `jev-content-packaging-shadow-v2`, its API call, contract versions, authentication, and calibration. The automation project must not copy its prompts, call TypeSafe directly, persist its shadow scores as approvals, or assume that a text-only thumbnail-copy score inspects the rendered image. It may read a future versioned judgment record through an internal adapter when the System One owner makes one available. A missing, stale, or shadow-only judgment is `advisory_unavailable`, never `approved`.

The P0-3 shadow route is limited to DEV/Preview and returns no durable decision. Its branch PR #61 is stacked on PR #43. Do not bind a production worker to that route or merge/rebase those branches as part of this project.

## Boundary with the content-flow project

Another development session owns the OS content flow: topics, packaging, knowledge script documents under the script root, document-to-content links (`system-one/production-document`), document status, content evidence, and the content workspaces and pipeline panel. This project must not change those files, APIs, statuses, or document names. Its own surface is `lib/server/youtube-*`, `lib/youtube-*`, `tools/render-youtube-scene.mjs`, `app/api/v1/content/youtube-automation/*`, and the private YouTube storage migrations. Earlier small edits in shared files (pipeline panel label and scene summary, voice-job guards in the record routes) stay minimal and are not extended.

Owner direction (2026-09-24): the real production process is the knowledge-document flow (00_기획 → 00_패키징 → 02_자료찾기 → 02_축확정 → 03_영상설계표 → 05_원고 and review documents) driven by canonical procedure documents. The target automation lets AI author those step documents from knowledge insights and advance them only to `review`; a person marks the approved version (`reviewed`/`canonical`). File names are never approval. Video production starts from that approved document version, and team members assigned to the content, not only the owner, may start generation and open private outputs, while approvals stay with the OS approvers.

Contract requested from the content-flow project, consumed read-only by this automation:

1. For a content item, the human-approved script document: document ID, version, content hash, approval status, approver and time.
2. The selected title and thumbnail copy with their version.
3. A listing of content items whose approved script has no current video run.
4. The team assignment used for staff access.
5. Where AI-authored step documents are written and which statuses AI may set.

Until that contract exists, the connected pipeline path stays DEV-only and this project does not write step documents or approvals.

## Versioned run contract

An automation run has a stable run ID and source ID. Capture script ID/version, selected package ID/version, source input digest, OS rule IDs/versions, prompt/model versions, voice reference fingerprint, and media template version before external work. A changed source or artifact makes the run stale. Every stage writes its own status, outputs and error category. Provider request IDs and media-template version remain future work. Retrying a voice stage reuses completed output for the same input key. Never issue a second YouTube `videos.insert` when a video ID or resumable session exists; verify the existing upload first.

Stages: `planned` → `voice_pending` → `voice_ready` → `scene_pending` → `assets_ready` → `render_pending` → `render_ready` → `review_pending` → `review_ready` → `private_upload_pending` → `private_uploaded`. Every stage can enter `needs_input`, `failed`, or `stale`. `private_uploaded` is set only after YouTube confirms channel and actual `private` visibility and the OS publication record is saved.

## Gate policy

1. Existing OS gate 1 must approve the current topic and package before the script is generated.
2. Existing OS gate 2 must approve the current script before voice generation. The selected package promise and script must agree. Missing evidence or conflicting rules pauses the run.
3. The media worker may create draft assets without granting publication authority. It must compare transcript, scene timing, asset manifest and render output; Sol reviews the finished audio and video.
4. Existing OS gate 3 must approve the current final video and publication kit before upload. A dedicated worker authorization may upload `private` only. The existing browser admin upload route remains unchanged.
5. JEV scores remain advisory until the System One project's own validation and promotion contract explicitly allows a particular judgment to affect a particular gate. A confidence number alone is never a publication approval.

## Provider and data boundaries

Fish API key and voice reference ID stay server-side. The voice model should be private. Send the minimal narration segment needed to synthesize each clip; do not send all OS knowledge or sensitive internal notes. Keep generated audio, images and video in private storage with owner-scoped access and a defined retention policy. Use an explicit synthetic-content disclosure field when YouTube's realistic-content rules apply. Renderers and generator models are replaceable adapters; do not make the job state depend on a particular video API.

## Delivery slices

1. Readiness and version contract; Fish transport with bounded input and explicit errors. The current development pilot generates only the first approved script paragraph on an explicit preview click.
2. Durable job queue and worker for paragraph-level voice generation and private asset storage. New jobs request Fish MP3 and word alignment in one timestamped TTS stream. Audio is stored in `os-youtube-voice`; the corresponding transcript timing is stored in the separate private `os-youtube-voice-timing` bucket. The timing artifact records the exact generated MP3 SHA-256. The provider-neutral render gate later binds the assembled final audio to its own SHA-256 and requires the normalized transcript to match the approved script exactly. It resolves each visual beat's spoken anchor into a render timeline; missing, reversed or too-short cues block rendering. A raw ASR result with recognition errors cannot bypass this gate.
3. Opus scene plan, repeatable character and diagram templates, optional evidence-backed still generation, captions and render worker.
4. Automated multimodal review, targeted repair loop, and scoped private-only YouTube worker upload.
5. Topic scheduling and outcome analysis after public release, using the current OS metrics rules and real observed windows.

No stage is reported as available merely because its adapter or UI exists. Connected DEV, Preview QA, and Production activation are separate gates.

## Current development pilot

Implemented in this branch: owner-scoped readiness, Opus scene planning from four live OS rules, one Fish voice preview, one generated still preview, and a durable Claude-reviewed voice job. The job captures script and package versions, the scene-plan version, rule versions, a fingerprint of the private voice ID, and hashes of narration paragraphs. The exact voice ID and full script are not copied into job metadata. It uses one `ai_job` record per input key, a compare-and-swap lease, one Claude Sonnet 5 pronunciation review, then one Fish paragraph per worker call. New runs use Fish's timestamped TTS stream; the parser appends every audio chunk, keeps the latest alignment snapshot per `chunk_seq`, and applies each chunk's absolute offset. MP3 and word timing use deterministic paths in distinct private buckets. A missing timing artifact blocks an already stored MP3 from advancing. Older jobs without `timingMode` retain the legacy MP3-only path; they cannot claim to have render-ready word times. The owner can request a 60-second signed MP3 playback URL. A failed retry can resume the same job after its configuration is corrected, while changed source inputs require a new job. JEV remains advisory and is not called by this worker.

This code is closed unless `YOUTUBE_AUTOMATION_PILOT_ENABLED=true`, `OS_ENVIRONMENT=development` or `qa`, `NEXT_PUBLIC_DEMO_MODE=false`, and the configured DEV Supabase project reference exactly matches the server URL. Production project references must differ. On Vercel, only a Preview deployment from a non-main branch may pass the gate. To operate the voice stage, configure `YOUTUBE_SCENE_RULE_IDS`, server-only `ANTHROPIC_API_KEY` (or `CLAUDE_API_KEY`), `FISH_API_KEY`, `FISH_VOICE_REFERENCE_ID`, optional `FISH_TTS_MODEL`, `SUPABASE_SERVICE_ROLE_KEY`, and `CRON_SECRET` in the verified DEV/Preview environment. The additive private-bucket migration `20260923070000_youtube_voice_media.sql` was applied to the isolated DEV project on 2026-09-23 after explicit user approval; the SQL Editor transaction also recorded the migration history. Readback showed `public=false`, 12 MiB file limit, MP3-only MIME restriction, one migration statement, and a statement checksum matching the committed file. Production was not changed. A DEV scheduler must call `POST /api/v1/content/youtube-automation/worker` with `Authorization: Bearer <CRON_SECRET>` repeatedly; one call advances at most one step. No scheduler is registered in `vercel.json`, and the worker has not been run against a connected DEV environment. The existing Preview build alone does not prove voice synthesis works.

The private timing bucket migration `20260924010000_youtube_voice_timing.sql` was applied to isolated OS DEV on 2026-09-24 after user approval, in the same transaction as its migration history entry. Readback confirmed `public=false`, a 1 MiB limit, JSON-only MIME restriction, and one stored statement whose MD5 matches the committed migration file. Production was not changed. After a TypeScript narrowing fix (`d49b1c5`), Vercel Preview for the PR branch built successfully; this does not establish that the connected voice worker has run. The local private preview demonstrates 25.6 seconds of content-only, speech-led character and diagram motion, with approximate pause-based timing. It does not prove word-level synchronization. The scene plan gives each visible headline a `typographyAnchor` copied from the approved script. `lib/server/youtube-timeline.ts` resolves that anchor against the final audio's verified word start and requires the drawing to begin first; it no longer guesses headline timing from a fixed delay. `tools/render-youtube-scene.mjs` is the offline renderer. It verifies the audio hash and duration, the plan-to-timeline binding, every beat's SVG, and each used character's checksum; it compresses a drawing authored at natural pace into its beat, then checks every beat's final frame in Chromium for elements outside the safe area, overlapping labels, labels over character art, and an over-wide headline before encoding. `--check-only` runs those checks without encoding, for a repair loop. Text uses Pretendard 1.3.9 (OFL-1.1), fetched from a pinned jsDelivr URL and checked by SHA-256, so macOS development and the Linux media worker render identically; rendering stops if the font does not load. The renderer has no external calls and cannot execute in the Vercel request runtime. The connected media worker still must assemble paragraph audio, verify each stored MP3 against its timing artifact, measure final duration and segment offsets, calculate final-audio SHA-256, supply the brief, and store the rendered MP4 privately. Production thumbnail, timed captions, final review and YouTube upload remain unimplemented. A private character manifest and owner-scoped asset storage are still required for connected renders. Final brand typography and colors remain adjustable. Voice files are retained privately until a retention policy and cleanup worker are implemented, so connected use should use controlled pilot data.

On 2026-09-24 the owner moved this project's text models from GPT to Claude: scene planning uses `claude-opus-5-5` at effort `high` and voice review uses `claude-sonnet-5`. The shared Claude transport omits `temperature` for models that reject sampling parameters and stops on `refusal`. A stored scene plan from another model is regenerated rather than reused. Voice jobs include the review model in their run key; a pre-switch job whose review has not run is marked stale instead of being reviewed by a different model. Company topic, script and embedding flows outside this project are unchanged.

Optional server env `FISH_TTS_SPEED` (0.5–2, sent as `prosody.speed`), `FISH_TTS_TEMPERATURE` and `FISH_TTS_TOP_P` (0–1) tune delivery; unset values keep Fish defaults. They are part of the voice run key and stored in job metadata, so changing them starts a new job instead of mixing deliveries. The owner's preferred direction is Fish `drama-3-preview` with about 12% faster speech; that model is a Fish preview whose support for the timestamp stream and prosody has not been verified with a real call. Shortening pauses between sentences is not implemented; it belongs to final audio assembly, where word times must be shifted with the removed silence.

The character bucket migration was applied to isolated OS DEV (`brandyaction-os-dev`) on 2026-09-24 after owner approval, in one SQL Editor transaction with its migration history entry. Readback: `public=false`, 10 MiB limit, PNG/JSON MIME list, one stored statement whose MD5 matches the committed file. The private catalog and the 53 usable PNGs were uploaded through the dashboard; readback found 54 objects whose name-and-size digest matches the local source set. Production was not changed. Scene generation still needs `ANTHROPIC_API_KEY`, the scene rule IDs and the pilot gate on a Preview deployment of this branch; it has not run against a connected environment.

The render stage is a thin-worker protocol. A finished voice run gets one deterministic `youtube_narration_render_v1` job. The media worker (`tools/youtube-media-worker.mjs`, on the owner's Mac during development and Vercel Sandbox later) holds only `OS_URL` and `YOUTUBE_MEDIA_WORKER_SECRET`, never database or provider keys. It calls `POST /api/v1/content/youtube-automation/media` to claim a leased job and receive signed segment URLs, stitches the MP3 segments with a stream-copy concat, uploads the narration, and asks for the brief. The OS downloads the stored narration and every segment, verifies them against their timing artifacts and the approved script, aligns the scene plan to verified word times, and returns the brief, the private catalog and signed URLs for only the characters used. The worker renders, uploads the MP4 through a signed URL, and completes with its size and SHA-256; the OS checks the stored size. Failures return a code and retry up to three times. Anyone whom the OS lets read the content (row-level security on the content record) can open the latest rendered video through `GET /api/v1/content/youtube-automation/video?sourceId=` as a 10-minute private URL, so assigned staff, not only the owner, can review it. Outputs use the new private `os-youtube-video` bucket (migration `20260924150000_youtube_narrated_video.sql`). A local run against a stub OS verified claim, stitch, brief, render, upload and completion; the protocol has not run against a connected environment.

On 2026-09-24, with owner approval, the video bucket migration was applied to isolated OS DEV (readback: `public=false`, 2 GiB bucket limit, MP4/MP3 MIME list, one stored statement whose MD5 matches the committed file). The DEV project's global upload limit is 50 MB while its spend cap is on, which covers roughly 20 minutes of video at the current encoding. Branch-scoped Preview settings for `codex/youtube-automation-20260923` now hold the pilot gate values, DEV and Production project references, `FISH_TTS_MODEL=drama-3-preview`, `FISH_TTS_SPEED=1.12`, and owner-entered secrets (Anthropic, Fish, Fish voice reference, media worker secret); Production settings were not changed. After a redeploy of `3cdc123`, the Preview health check reported content AI ready, an unauthenticated worker call returned `INVALID_WORKER_SECRET` (pilot gate open), and an authenticated claim from the owner's Mac returned an empty queue. No voice, scene or render job has run yet: DEV has no approved script, and `YOUTUBE_SCENE_RULE_IDS` is not configured.

Temporary approved-script bridge (owner decision 2026-09-24, pending the content-flow contract): every automation step reads its input through `readYoutubeAutomationInput`. A content record may carry `metadata.scriptDocument` (`POST /api/v1/content/youtube-automation/document-source`, DEV pilot only); the automation then requires that knowledge document's status to be `reviewed` or `canonical` (set only through the audited `os_set_document_status`), reads the exact version from `os_document_versions`, keeps only spoken paragraphs, and binds the input key to the document ID, version and content SHA-256. Any edit or status change makes the run stale; a new version needs a new binding. No content-flow file, status or document is changed by this bridge. Packaging is optional in this mode and voice-run metadata keeps it optional.
