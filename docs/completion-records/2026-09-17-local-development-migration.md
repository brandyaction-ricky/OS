# BrandyAction OS 로컬 개발환경 이전 점검 기록

- 점검 일자: 2026-09-17 (Asia/Seoul)
- 저장소: `brandyaction-ricky/OS`
- 로컬 경로: `/Users/ricky/Projects/brandyaction-os`
- 작업 브랜치: `codex/execution-setup-20260917`
- 작업 전 로컬 HEAD: `400c6b3356d8eb8bc33c637e64a4d1f76010e0dd`
- 원격 기준: `origin/main` @ `0661eb4fe3aed673dae53b41ed8ab7ea9d6229c2`
- 변경 요약: 원격 main의 프로젝트 컨텍스트와 이전 가이드를 통합했다. Node 버전 고정, 안전한 로컬 설정 생성, 환경 검사, 단일 검증 명령, CI 게이트, 아키텍처·환경 문서를 구축했다. 일반 빌드에서 Telegram webhook 변경을 제거하고 별도 확인 명령으로 분리했다. 이어서 Playwright 로컬 데모 스모크 테스트와 연결형 인증 테스트 골격을 추가하고, PostCSS 보안 수정 버전을 고정하고, 원격 Vercel/Supabase 상태를 읽기 전용으로 점검했다. Production schema-only snapshot을 CLI 생성 기준선으로 만들고 기존 14개 delta를 체크섬 그대로 보존했으며, 기준선의 로컬 zero-state 재구축을 통과시켰다. public-only export에서 빠진 Auth 프로필 트리거를 순방향 migration으로 복원하고, RLS 성능 경고 18개를 의미 보존형 정책 migration으로 해소했으며, 트랜잭션 롤백형 pgTAP RLS 테스트 20개를 추가했다.
- 원격 동기화 상태: 원격 main을 병합한 로컬 작업 브랜치를 동일한 이름으로 push했고, 로컬 브랜치는 원격 작업 브랜치를 추적한다. `main` 병합은 수행하지 않았다.
- 문서: `PROJECT_CONTEXT.md`, `CODEX_MIGRATION.md`, `ARCHITECTURE.md`, `docs/ENVIRONMENTS.md`가 현재 작업 트리에 존재한다.
- 실행 환경: Node.js `v24.21.0`, npm `11.19.0`, Supabase CLI `2.117.0`(exact pin), Docker Desktop `4.91.0`, PostgreSQL 17 로컬 설정, `.env.local` 데모 모드(권한 600)
- 검증 완료: `npm ci`, `npm run setup:local` 생성·비덮어쓰기, `npm run env:check`, `npm run verify`(232/232 테스트·린트·타입 검사·빌드 통과), `npm audit` 취약점 0건, Supabase CLI·PostgreSQL 버전·Data API·seed 설정 검사 통과, 공식 schema-only Production dump의 행 데이터·자격증명 부재 확인, active migration 3개 전체 대상 `supabase db reset --local --no-seed` zero-state 재구축 성공, migration 이력 3개 확인, 재구축 직후 `supabase test db` 20/20 통과, 공식 Supabase Security/Performance Advisor warning 이상 0건, Production/Local 객체 inventory 일치, 정규화된 생성 TypeScript 타입 SHA-256 일치, `npm run db:migrations:verify` 무결성 통과·원격 적용 준비 차단 유지, Playwright Chromium 로컬 데모 `/home` 렌더링·콘솔 오류 부재·`/api/v1/health` 계약 확인
- 검증 미수행: 실제 Supabase/Auth 로그인, 외부 AI, YouTube, Telegram, 광고 연동, DEV Preview의 인증 사용자 흐름. DEV 프로젝트는 비어 있고 환경변수·테스트 계정이 아직 없어 연결형 인증 테스트는 1건 skip했다.
- 보안 점검: `postcss`를 수정 버전 `8.5.28`로 고정했고 `npm audit`는 알려진 취약점 0건이다. 전체 active chain을 새로 적용한 로컬 DB에서 공식 Supabase Security Advisor와 Performance Advisor는 warning 이상 0건이다. 기존 performance warning 18개(인증 함수 init-plan 13개, 중복 permissive 정책 5개)는 별도 forward migration으로 해소했고, 권한 동작은 pgTAP 20개로 재검증했다. Production 변경은 수행하지 않았다.
- 로컬 커밋: `0db8b54`(원격 문서 통합), `337919a`(로컬 환경과 릴리스 게이트), `a5ef2a6`(이전 준비상태 기록), `b31e56c`(브라우저 QA·보안 패치·원격 환경 게이트), `255e154`(격리된 Supabase DEV 프로젝트 상태 기록), `e3430d4`(Supabase migration 기준선 안전장치), `827fc00`(migration 기준선 게이트 기록), `1e02c65`(Supabase schema export 도구 준비), `ce3ec22`(schema export 전제조건 기록), `6276692`(로컬 검증된 Supabase schema baseline), `0c21712`(검증된 DB 기준선 완료 기록), `d663374`(Auth 트리거 복원·RLS 최적화·pgTAP 회귀 테스트), `495b469`(전체 migration zero-state 검증 완료 기록)
- 원격 PR: [#41](https://github.com/brandyaction-ricky/OS/pull/41) · `codex/execution-setup-20260917` → `main` · open · 미병합
- 원격 CI: GitHub Actions `validate` run 145가 commit `827fc00`에서 성공했다. `npm ci`, 전체 검증, Chromium 설치, Playwright 스모크가 모두 통과했다. 이번 로컬 커밋들은 아직 원격 push 전이다.
- 배포: Vercel이 commit `827fc00`의 Preview를 자동 생성했고 `READY`에 도달했다. `/home`과 `/api/v1/health`는 HTTP 200이다. Preview health에서 database/auth가 `missing`이므로 연결형 QA는 아니다. 이번 로컬 변경은 아직 Preview에 배포되지 않았다. Production 배포·승격·변경은 수행하지 않았고, 최신 관찰 Production은 원격 `main`의 `0661eb4`로 ready 상태다.
- 원격 환경 점검: 로컬 checkout은 ignored `.vercel/project.json`으로 기존 프로젝트에 연결했다. 월 $0 비용을 확인·승인받아 서울 리전에 별도 `brandyaction-os-dev` 프로젝트를 생성했고, healthy 상태·public 테이블 0개·migration 0개·보안 advisor 0건을 확인했다. Production 이력 7개와 기존 저장소 delta 14개는 정확히 일치하는 식별자가 0개다. 읽기 전용 Production schema-only snapshot을 `supabase/migrations/20260917082749_core_baseline.sql`로 정리했고 기존 delta는 `supabase/migrations-legacy`에 체크섬 그대로 보존했다. 로컬 zero-state 적용 중 발견한 `pg_trgm` 확장 누락과 권한 비호환 함수 설정을 기준선 후보에서 수정했다. active chain은 기준선과 두 순방향 migration으로 구성되며 `supabase/migration-baseline.json` 상태는 `validated_local`, 결정은 `do_not_apply`다. Production과 DEV의 데이터·스키마·설정은 변경하거나 복사하지 않았다. Vercel 환경변수 scope는 아직 설정하지 않았다.
- 남은 작업: 별도 DEV 적용 승인 후 manifest를 `ready`/`apply`로 전환하고 DEV migration 적용, DEV/Preview Vercel 환경변수 scope 설정, 전용 테스트 계정으로 실제 Auth 및 인증 브라우저 테스트 실행. Production migration 이력 정리는 별도 계획과 명시적 승인이 필요하다.
- OS 운영 기록: 사용자 지침에 따라 운영 DB/API에 기록하지 않았다. 이 로컬 기록의 OS 반영은 보류 상태다.
