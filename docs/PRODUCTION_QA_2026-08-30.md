# Brandy OS 운영 QA 보고서

- 점검일: 2026-08-30 KST
- 대상: https://brandyaction-os.vercel.app
- 범위: 관리자 로그인 후 전체 주요 화면, 주요 열기·필터·탭·내비게이션 버튼, 비로그인 API 보안 경계

## 결론

전체 메뉴는 404 없이 열렸고, 기존 DB·인증·Telegram 연결은 유지됐다. 콘텐츠 7개 화면과 사람 승인 게이트도 운영 코드에 포함돼 있다. 실제 운영 데이터를 생성·변경·발행하는 버튼은 데이터 훼손과 외부 발행을 피하기 위해 폼 열기와 권한·계약까지만 확인했다.

이번 점검에서 사용자에게 동작하는 것처럼 보이지만 아무 반응이 없던 버튼과 지식 목록 초기 로딩 문제를 수정했다. 이후 원본 `studio` 전달본 전체와 다시 대조해 범용 CRUD로 남아 있던 콘텐츠 핵심 공정을 전용 화면과 API로 이식했다. 자동 검증은 37개 테스트, ESLint, TypeScript, Next.js 프로덕션 빌드가 모두 통과했다.

## 점검 범위

| 영역 | 화면 수 | 주요 확인 내용 | 결과 |
| --- | ---: | --- | --- |
| 홈 | 4 | 대시보드, 목표, 결정, 보고서 | 정상 |
| 콘텐츠 | 7 | 주제, 원고, 제목·썸네일, 숏폼, 발행, YouTube, 성과 | 정상 |
| 지식 | 5 | 작업공간, 검색, 검토, Skill, 그래프 | 수정 후 정상 |
| 조직운영 | 7 | 구성원, 업무, 회의, 일정, 연차, AI 작업, 경영지원 | 정상 |
| 성과관리 | 6 | 개요, KPI, 매출, 퍼널, 광고, 자사몰 | 화면명 수정 |
| 설정 | 6 | 연결, 권한, 감사, 모니터링, 회사, 채널 | 정상 |
| 공통 | 1 | 명령 팔레트, 알림, 프로필, 사이드바 | 수정 후 정상 |

## 수정 사항

| 우선순위 | 발견 내용 | 수정 결과 |
| --- | --- | --- |
| 높음 | 지식 1,611개를 모두 받은 뒤 목록을 표시해 수 초 동안 `0개`로 오해할 수 있음 | 첫 200개를 즉시 표시하고 나머지 페이지를 병렬 로딩하도록 변경; 로딩 문구 추가 |
| 중간 | 지식 목록의 `최근 수정순` 버튼이 눌려도 반응 없음 | 최근/오래된 수정순 토글 연결 |
| 중간 | 지식 문서의 더보기 아이콘이 눌려도 반응 없음 | 문서 정보 탭 열기로 연결하고 접근성 이름 추가 |
| 중간 | 전역 알림 버튼이 표시되지만 아무 반응 없음 | 알림 패널을 연결하고 가짜 미확인 점 제거 |
| 낮음 | 사이드바 `이 영역에 기능 추가`가 구현되지 않은 상태로 노출 | 오해를 막기 위해 제거 |
| 낮음 | 지식 검색 `상세 조건`이 구현되지 않은 상태로 노출 | 기본 상태 필터는 유지하고 무반응 버튼 제거 |
| 낮음 | 내비게이션은 `자사몰 어드민`, 본문은 `고객·주문 관리자`로 명칭 불일치 | `자사몰 어드민`으로 통일 |
| 낮음 | 설정 화면에서 사용하지 않는 회사 설정 요청과 상태가 남아 있음 | 불필요한 API 호출과 상태 제거 |

## 동작 확인한 주요 버튼

입력·수정 동작은 저장 직전까지 폼과 필수 입력 항목을 확인했다.

- 공통: 명령 팔레트 열기/닫기, 프로필 메뉴, 알림 패널, 모든 좌측 메뉴
- 홈: KPI 추가, 목표 추가, 결정 추가
- 콘텐츠: 콘텐츠 주제 추가, 원고 추가, 성과 기록 추가, 제목·썸네일 탭
- 조직운영: 업무 추가, 회의 기록, 회의 준비, 휴가 신청, 연차 부여
- 성과관리: 주간 KPI 입력, 매출 입력, 퍼널 입력, 관리 링크 추가
- 지식: 폴더 선택, 검색, 정렬, 문서 선택, 문서 정보

## 콘텐츠 반영 상태

초기 5차 반영에는 메뉴와 공통 데이터 계약은 있었지만, 전달본의 페이지별 동작을 다시 대조한 결과 `주제·기획`, `원고·스크립트`, `영상 성과`가 범용 CRUD 수준에 머문 누락이 확인됐다. 이번 후속 작업에서 다음을 보완했다.

- `정본 기반 생성 → 자가검수 → 사람 검토 → 최종 승인 → 예약` 상태 흐름
- 주제·기획, 원고·스크립트, 제목·썸네일, 숏폼 편집, 발행·업로드, YouTube 관리, 영상 성과 화면
- 외부 발행 전에 사람 승인을 요구하는 서버 계약
- 콘텐츠 AI 자격 증명이 없을 때 가짜 결과 대신 연결 대기 상태 표시
- 채널 모으기 → 터진 영상 발굴 → 틈새 확정 3단계와 기획 후보 채택·HANDOFF 저장
- 기획/패키징/자료/축 확정/설계표/초안/다듬기/발행 원고 8단계와 대표 결재 배너
- 제목·썸네일 후보의 실제 채택/해제 저장
- 조회·CTR·시청지속률·전환을 위계별로 보는 영상 성과 전용 화면
- 레거시 `/contents`와 섹션 루트 URL의 안전한 첫 화면 리다이렉트
- 페이지별 브라우저 제목, 시맨틱 브레드크럼, CSV 업로드와 구성원 선택 접근성 개선

운영 지식 작업공간에서 `02_Wiki/콘텐츠절차` 폴더의 정본 6개를 확인했다. `패키징_절차`, `현재기준`, `원고_다듬는_절차`, `원고_쓰는_절차`, `분석_절차` 등이 `content_studio` 태그와 함께 적재돼 있다. 제목·썸네일 화면의 검색·제목·썸네일·저장함 탭도 운영판에서 모두 동작했다. 따라서 콘텐츠 화면과 절차 정본이 모두 반영된 상태다.

## 미실행·외부 의존 항목

- `OPENAI_API_KEY` 미등록: 의미 임베딩과 일부 콘텐츠 AI 실행은 연결 대기
- 광고 API 자격 증명 미등록: Meta·Google 실데이터 동기화는 연결 대기
- 자사몰 관리 링크 미등록: 실제 외부 어드민 이동 미검증
- 운영 데이터 삭제, 최종 승인, 예약·외부 발행은 안전을 위해 실행하지 않음
- 실제 모바일 기기별 터치 QA는 별도 기기 점검 권장

## 자동 검증

- Node 계약·회귀 테스트: 37/37 통과
- ESLint: 통과
- TypeScript: 통과
- Next.js 프로덕션 빌드: 통과
- 운영 전체 37개 경로: 404 및 앱 출처 콘솔 오류 없음
- 운영 health: DB·인증·Telegram `ready`, 임베딩 `keyword_only`, 콘텐츠 AI·광고 `missing`
- 비로그인 API 보안: 문서·운영기록·구성원·광고·Telegram 설정·인덱싱 모두 `401` 차단

## 후속 권장 사항

1. OpenAI와 광고 계정 자격 증명을 등록한 뒤 의미 검색·콘텐츠 생성·광고 동기화 E2E를 수행한다.
2. 실제 모바일 기기에서 360px/390px 너비의 입력 폼과 메뉴를 한 차례 확인한다.

## 운영 배포 및 회귀 검증

- GitHub PR: #12
- `main` 병합 커밋: `f47671e`
- Vercel Production: Ready (44초)
- 지식 작업공간: 1,611개 표시, 정렬 토글·문서 정보 정상
- 콘텐츠 정본: `02_Wiki/콘텐츠절차` 6개 확인
- 콘텐츠 패키징: 검색·제목·썸네일·저장함 탭 정상
- 전역 알림 패널·자사몰 화면명 정상
- 운영 앱 출처 콘솔 오류: 0건
### 콘텐츠 전달본 재대조 후속 배포

- 전달본: `브랜디OS_콘텐츠_전달_리키님(1).md`, 콘텐츠 문서 ZIP, 콘텐츠 코드 ZIP 전체 대조
- GitHub PR: #13
- `main` 병합 커밋: `f72a16e`
- Vercel Production: 성공
- 전용 화면: 주제·기획 3단계, 원고 8단계·결재, 영상 성과 판정
- 생성 계약: 기획 브리핑·원고 초안의 정본 실행 및 자가검수
- 후보 계약: 기획·제목·썸네일 채택 상태 저장
- 회귀 검증: 테스트 37개, ESLint, TypeScript, 프로덕션 빌드 통과

## 5개 세션 점검 파일 후속 개발

대상 파일은 `유튜브발행키트·발행자동화`, `숏폼발행세션`, `제목·썸네일·원고·주제기획 리키전달`, `제목·썸네일·원고·주제기획 세션`, `홈·조직·성과·설정·지식 세션`의 2026-08-30 점검 ZIP이다. 최신 `origin/main`과 다시 비교해 이미 해결된 항목은 유지하고 아래 누락을 추가 개발했다.

### 반영 완료

- 주제·기획, 원고 8단계, 영상 성과를 일반 CRUD가 아닌 전용 업무 화면으로 연결
- 제목·썸네일 후보 채택·해제 저장 및 `검색 → 제목 → 썸네일 → 저장함` 단계 표시
- 서버 전용 YouTube Data API 시장 검색 추가: 제목·썸네일·채널·조회수 근거를 가져와 제목 패키징 생성 입력으로 전달
- 파생 채널을 `쇼츠·Threads·SEO 칼럼·인스타 카드뉴스·에세이`로 통일
- SEO 칼럼 생성 계약에 `deriv_html`을 추가하고 목차·JSON-LD·hero·영상 임베드 포함을 요구
- 검토·발행 대기목록에 채널 필터, 마감/단계/채널 정렬, 영상별/채널별 묶기, 단계 표시, 예약 일시 선택, 일괄 검토·일괄 예약 연결
- 원본 롱폼 보관 기능과 클릭 가능한 현황 카드 연결
- 유튜브 발행 키트의 사용 순서 3단계, 수정 모드, 수정 저장 연결
- 지식 작업공간 집중 모드와 폴더·문서 목록 개별 접기, 접힘 상태 기억, 집중 모드용 상단 지식 탭 추가
- 대표 확정 사항에 따라 `프로젝트 관제`를 내비게이션에서 제거하고 기존 URL은 업무 화면으로 리다이렉트
- `/content`, `/contents`, `/performance`, `/settings` 루트 호환 리다이렉트와 페이지별 제목·시맨틱 브레드크럼 유지

### 키 연결 후 실검증 필요

- Claude 정본 실행: 주제 브리핑, 원고 초안, 제목·썸네일, 쇼츠 구간, 파생물, 유튜브 발행 키트의 실제 생성 품질과 자가검수 결과
- YouTube Data API: 운영 환경에서 시장 검색 쿼터·키 제한·응답 시간 확인
- 파생 데이터 생성 후 쇼츠 구간 채택 → 제작 대기, 검토 → 승인 → 예약 전체 흐름
- SEO 칼럼의 실제 `deriv_html` 구조와 자사몰 게시기 전달 계약

YouTube Data API 키로는 공개 영상 검색·조회수 등은 읽을 수 있지만 CTR·시청지속률과 실제 업로드는 검증할 수 없다. CTR·지속률은 YouTube Analytics OAuth가, 업로드는 OAuth 사용자 승인과 갱신 토큰이 추가로 필요하다.

### YouTube OAuth 업로드 구현

- 관리자 전용 Google OAuth 연결·콜백·연결 해제 API 추가
- `youtube.upload`, `youtube.readonly` 범위와 offline 갱신 토큰 사용
- OAuth `state` 서명·10분 만료·HttpOnly/SameSite 쿠키로 CSRF 방어
- 갱신·접근 토큰은 AES-256-GCM 암호화 후 사용자별 Supabase 테이블에 저장하며 브라우저·로그에 반환하지 않음
- 최종 승인 체크 후 비공개/일부공개만 허용하고 YouTube resumable 세션으로 브라우저→Google 직접 업로드
- 업로드 결과의 채널 소유권을 재검증하고 발행 기록과 영상 URL 저장
- 운영 적용에는 `202608300007_youtube_oauth.sql`, OAuth 환경변수, Google Cloud 승인 리디렉션 URI 등록이 필요

### 후속 자동 검증

- Node 계약·회귀 테스트: 37/37 통과
- ESLint: 경고 없이 통과
- TypeScript: 통과
- Next.js 프로덕션 빌드: 통과
- 새 API 경로: `/api/v1/youtube/search`
- 비밀값 저장: 코드·문서·클라이언트 응답에 API 키를 포함하지 않음

### 로컬 콘텐츠 데이터 동기화 후속 개발

5개 세션 문서를 최신 `main`과 다시 대조한 결과, 화면·정본·승인 공정은 반영됐지만 로컬 `radar.db`의 기존 콘텐츠를 OS로 옮기는 정식 경로가 남아 있었다. 아래를 추가했다.

- 관리자 전용 `/api/v1/content/import` 추가
- `source_content` → 원본 롱폼, `derivative_content` → 검토·발행 대기목록, `kpi_snapshots` → 영상 성과로 매핑
- 로컬 ID를 `metadata.legacyId`로 보존해 동일 스냅샷 재업로드 시 중복 생성 대신 갱신
- 원본이 없는 파생 항목은 건너뛰고 결과 건수로 명시
- 가져온 파생 항목에도 사람 검토·최종 승인·예약 게이트 유지
- `scripts/export-radar-snapshot.py`로 실제 전달본 SQLite 스키마를 OS JSON 계약으로 변환
- 발행·업로드 화면에 관리자 전용 `로컬 자료 가져오기` 추가
- 5MB·원본 1,000·파생 3,000·성과 3,000건 제한과 서버 스키마 검증

검증 결과: 합성 SQLite의 원본·파생·성과 변환 통과, Node 테스트 42/42, ESLint, TypeScript, 프로덕션 빌드 통과. Google OAuth 계정 연결과 실제 외부 업로드는 사용자 요청에 따라 별도 단계로 유지한다.

### Claude 운영 실호출 후속 수정

운영 제목·썸네일 화면에서 기존 QA 원본 1건으로 Claude 정본 실행을 1회 호출했다. 환경변수와 Anthropic 요청은 정상 동작했지만, 자유 형식 JSON 응답이 파서 계약과 맞지 않아 `CONTENT_GENERATION_INVALID`로 종료되는 문제를 재현했다.

- Anthropic Messages API의 `output_config.format.type=json_schema` 구조화 출력 적용
- 기획·원고·제목 패키지·쇼츠·파생·유튜브 키트별 JSON Schema 분리
- 두 번 생성하던 초안→검수 호출을 정본 기반 단일 구조화 호출로 합치고, 제출 전 자가검수·수정·점수·검토 결과를 같은 응답에 포함
- 콘텐츠 유형별 출력 토큰 상한을 7,000~14,000으로 조정
- `stop_reason=max_tokens`를 별도 오류로 처리해 잘린 JSON을 일반 형식 오류로 오인하지 않게 개선

수정 후 Node 테스트 42/42, ESLint, TypeScript, 프로덕션 빌드 통과.

첫 구조화 출력 배포의 운영 재호출에서는 Anthropic API가 `400`을 반환했다. 공식 JSON Schema 제한과 대조해 raw schema의 `minimum`·`maximum`이 지원되지 않는 제약임을 확인했다. 수치 범위는 설명과 저장 전 검증으로 유지하고, Claude에 전달하는 스키마에서는 해당 키워드를 제거했다.

최종 운영 재검증에서는 같은 QA 원본으로 Claude 정본 실행이 정상 완료됐다. 제목 후보 8개가 생성됐고 후보 설명·훅·자가검수 점수와 채택 상태가 화면 및 저장 계약에 맞게 표시됐다. 앱 콘솔 오류는 0건이었으며, 운영 헬스에서 DB·인증·Claude·YouTube·YouTube OAuth·Telegram이 모두 `ready`로 확인됐다. Google 계정 승인과 실제 채널 업로드는 사용자 요청에 따라 별도 연결 단계로 남긴다.

### Google OAuth 계정 연결 및 비콘텐츠 세션 재대조

Google Cloud 웹 OAuth 클라이언트와 운영 환경을 연결하고 승인 흐름을 실제로 완료했다. 비밀값과 토큰 원문은 브라우저 응답·로그·보고서에서 조회하거나 기록하지 않았다.

- 승인 리디렉션 URI: `https://brandyaction-os.vercel.app/api/v1/youtube/oauth/callback`
- 운영 재배포: Ready, 운영 도메인 연결 확인
- OAuth 콜백: `GET /api/v1/youtube/oauth/callback` → `307` 정상 완료
- 연결 채널: `안저`, 화면에 `OAuth 업로드 권한 연결됨` 표시
- DB 검증: 채널 ID 저장, 갱신 토큰 저장, `v1.` 암호화 형식, 접근 토큰 만료시각 저장이 모두 `true`
- 발행 키트: QA 원본으로 `POST /api/v1/content/generate` → `201`, 제목·설명·태그·챕터·채널별 문안 생성 확인
- 실제 영상 업로드: 승인된 영상 파일이 전달되지 않아 비공개 업로드와 URL·발행 기록 저장 검증은 대기

첨부 `홈·조직·성과·설정·지식세션_2026-08-30` ZIP을 `main`과 운영 화면에 다시 대조했다. 집중 모드와 프로젝트 관제 메뉴 제거는 이미 반영돼 있었고, 아래 잔여 누락을 추가 수정했다.

- 홈·지식·조직·성과·설정 및 공통 화면의 영어 대문자 소제목을 쉬운 한글로 변경
- 회의 참석자 예시 `리키, 데이빗, 에릭`을 실제 팀 기준 `안저, 리키, 에릭`으로 변경
- AI 작업 빈 상태의 `프로젝트 관제에서 요청서` 안내를 현재 `AI 작업에서 요청서 등록` 흐름으로 변경
- 제거된 `/organization/projects` 설정 잔여를 삭제하고 기존 URL의 업무 화면 리다이렉트는 유지
- 회사 설정에서 대상 계정의 표시 이름이 `안저`로 저장·표시되는 상태 확인
- 지식 집중 모드, 폴더·문서 목록 개별 접기, 상태 기억, 상단 지식 탭의 기존 구현을 회귀 검증

후속 자동 검증은 Node 테스트 43/43, ESLint, TypeScript, Next.js 프로덕션 빌드가 모두 통과했다.

### 성과관리 OS 1차 전달본 후속 개발

첨부 `성과관리_OS_1차.zip`의 11개 미해결 요청을 최신 `main`과 대조했다. 소제목 한글화는 이미 완료돼 유지했고, 나머지 항목을 아래와 같이 반영했다.

- 성과관리 6개 페이지가 공유하는 브랜드·기준월 필터를 스테이지 상단에 추가하고 브라우저에 선택 상태를 저장
- 퍼널을 고정 4단계에서 단계 추가·삭제·이름·값·순서 변경이 가능한 구조로 전환; 기존 기록은 호환 표시
- 주간 KPI와 자사몰 어드민에 입력 전 안내 빈 상태 추가
- 광고 성과의 광고비·전환 매출·CPA·교차 확인 금액을 만원 단위로 통일
- 광고 계정 미연결 상태에서 API 동기화 시 설정 위치를 알려주는 한글 오류 안내 추가
- `브랜드 관리자`를 `자사몰 어드민`, `매출 원장`을 `매출 상세`로 통일
- 매출의 오늘·7일·30일·이번 달·직접 선택 필터와 일별 추이·유입원별 집계 추가
- 매출 CSV와 광고 CSV 초기 적재 추가; 서버에서 최대 2,000행·날짜·브랜드·비음수 수치를 검증하고 광고 CSV는 관리자만 허용
- KPI 이상신호 규칙을 목표 대비 10%, 전환 지표 전주 대비 20%, 일반 지표 전주 대비 10%, 2주 연속 0으로 통합
- 같은 이상신호 함수를 성과 통합 현황과 회의 준비가 함께 사용하도록 연결
- 존재하지 않는 `/performance/*` 경로는 성과 통합 현황으로 리다이렉트

개발 자동 검증은 Node 테스트 48/48, ESLint, TypeScript, Next.js 프로덕션 빌드가 모두 통과했다. 실제 운영 CSV 적재는 원본 파일이 없고 운영 데이터 변경을 피하기 위해 실행하지 않았으며, 광고 API 실동기화는 자격 증명 등록 후 검증 대상으로 유지한다.

운영 반영 및 화면 재검증 결과:

- GitHub PR: #20
- `main` 병합 커밋: `e7f2d5f4326f4613b06a80b2b02dd0f9d00ddd78`
- Vercel Production: Ready (37초)
- 미리보기 첫 빌드에서 대용량 CSS 전송 인코딩 오류를 발견해 원본 Git 객체 해시와 일치하도록 교정한 뒤 재배포 완료
- 운영 6개 성과관리 화면에서 공통 필터, 빈 상태, 만원 단위, 매출 분석, CSV 진입점, 퍼널 단계 편집 진입점 확인
- 광고 자격 증명 미등록 상태의 API 동기화 버튼에서 설정 위치 안내 확인
- 잘못된 `/performance/*` 경로가 성과 통합 현황으로 이동하는 것 확인
- 운영 앱 출처 콘솔 오류: 0건
- 운영 레코드 생성·CSV 적재·광고 API 호출은 실제 데이터 변경을 피하기 위해 실행하지 않음

### 홈 OS 1차 전달본 후속 개발

첨부 `홈_OS_1차.zip`의 홈 4개 화면 요청을 최신 `main`과 대조했다. 소제목 한글화·표시 이름·검색·버튼·모바일 기본 반응형은 이미 반영돼 있었고, 로그인 화면도 `팀 로그인`으로 확인됐다. 남은 요청과 대조 중 발견한 매출 중복 합산 가능성을 아래와 같이 수정했다.

- 목표값 또는 현재값이 없는 KPI는 100%나 0%가 아닌 `측정 전`으로 표시
- 목표 카드도 연결된 측정 KPI와 자체 기준값이 모두 없으면 `기준 미설정`으로 표시하고 진행 막대를 채우지 않음
- 목표·KPI와 월간 보고서가 같은 측정 전용 달성률 함수를 사용하도록 통일
- 월간 보고서의 `MD 내려받기`를 `글 파일로 내려받기`로 바꾸고 UTF-8 텍스트 파일로 저장
- 콘텐츠 부모 관계와 생성 원본 ID를 따라 같은 영상의 기획·원고·패키지·발행 기록을 한 줄로 묶고 현재 단계를 뱃지로 표시
- 홈 매출을 성과관리 일자별 순매출 기록에 연결하고 통합·마이인·브랜디액션 에듀로 집계
- 목표·KPI에 등록된 월별 금액 목표를 브랜드별 매출 목표로 연결하고, 미설정 시 목표 화면 진입점 표시
- 이번 달 실적의 전월 대비와 최근 7일의 직전 7일 대비를 각 카드에 표시; 비교 기준값이 없으면 가짜 0% 대신 `비교 전` 표시
- 가장 최근 이번 달 매출 갱신 시각을 홈 설명에 표시
- CSV 매출처럼 `metadata.net`과 `amount`가 함께 저장된 기록을 두 번 더하던 홈 합산 가능성을 제거
- 대시보드와 월간 보고서가 필요한 기록 유형을 각각 별도로 불러와 전체 200건 제한 때문에 최근 매출·KPI가 누락될 가능성을 줄임

개발 자동 검증은 실제 계산 단위 테스트를 포함해 Node 테스트 56/56, ESLint, TypeScript, Next.js 프로덕션 빌드가 모두 통과했다. 목표·매출·콘텐츠 운영 기록은 검증 중 변경하지 않았다.

운영 반영 및 실데이터 재검증 결과:

- GitHub PR: #21
- `main` 기능 병합 커밋: `d58a694c4708d5116953100608bab4549b7b448c`
- Vercel Production: Ready
- 운영 목표 `매출 1억`: 기존 100% 진행 막대가 사라지고 `기준 미설정` 표시
- 목표·KPI 상단과 월간 보고서 KPI 달성률: 모두 `측정 전`으로 일치
- 자동 생성 월간 보고서 본문: `목표·KPI 평균 달성률: 측정 전` 확인
- 월간 보고서 버튼: `글 파일로 내려받기` 확인
- 홈 `이번 주 영상`: 반복되던 QA 영상이 한 줄로 통합되고 현재 단계 `발행 키트` 표시; 별도 테스트 영상까지 총 2편으로 정리
- 매출 기록이 없는 운영 상태: 각 카드 `0만원`, `전월 비교 전`, `전주 비교 전`, `목표 미설정`, `목표 설정하기`를 사실대로 표시
- 운영 앱 출처 콘솔 오류: 0건
- 운영 목표·매출·콘텐츠 기록 생성·수정 없음
