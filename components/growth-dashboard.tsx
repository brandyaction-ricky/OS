"use client";

import { ArrowRight, BarChart3, CircleAlert, CircleDollarSign, Filter, Link2, TrendingUp } from "lucide-react";
import Link from "next/link";
import { useCallback, useEffect, useMemo, useState } from "react";
import { listRecords } from "@/lib/api-client";
import type { OsRecord } from "@/lib/record-types";
import { useSession } from "./session-provider";

function currentMonth() { const now = new Date(); return `${now.getFullYear()}-${String(now.getMonth() + 1).padStart(2, "0")}`; }
function period(record: OsRecord) { const value = record.metadata?.periodMonth ?? record.metadata?.date; return typeof value === "string" ? value.slice(0, 7) : record.starts_at?.slice(0, 7) || record.created_at.slice(0, 7); }
function manwon(value: number) { return `${(value / 10_000).toLocaleString("ko-KR", { maximumFractionDigits: 1 })}만원`; }

export function GrowthDashboard() {
  const { accessToken, demo } = useSession(); const [month, setMonth] = useState(currentMonth); const [records, setRecords] = useState<OsRecord[]>([]); const [error, setError] = useState("");
  const load = useCallback(async () => { if (demo) return; try { const results = await Promise.all(["revenue", "funnel", "kpi", "connection"].map((type) => listRecords(accessToken, type as "revenue" | "funnel" | "kpi" | "connection", "limit=200"))); setRecords(results.flatMap((result) => result.records)); setError(""); } catch (reason) { setError(reason instanceof Error ? reason.message : "성과 데이터를 불러오지 못했습니다."); } }, [accessToken, demo]);
  useEffect(() => { load(); }, [load]);
  const revenue = records.filter((record) => record.record_type === "revenue" && period(record) === month); const funnels = records.filter((record) => record.record_type === "funnel" && period(record) === month); const kpis = records.filter((record) => record.record_type === "kpi").slice(0, 8); const connections = records.filter((record) => record.record_type === "connection" && record.tags.includes("commerce-admin"));
  const actual = revenue.reduce((sum, record) => sum + Number(record.metadata.net ?? record.amount ?? 0), 0); const gross = revenue.reduce((sum, record) => sum + Number(record.metadata.gross ?? 0), 0); const orders = revenue.reduce((sum, record) => sum + Number(record.metadata.orders ?? 0), 0); const averageConversion = funnels.length ? funnels.reduce((sum, record) => sum + Number(record.metric_current ?? 0), 0) / funnels.length : 0;
  const brandRows = useMemo(() => [...new Set(revenue.map((record) => record.brand || "미분류"))].map((name) => ({ name, amount: revenue.filter((item) => (item.brand || "미분류") === name).reduce((sum, item) => sum + Number(item.metadata.net ?? item.amount ?? 0), 0) })).sort((a,b)=>b.amount-a.amount), [revenue]);
  return <><header className="page-header"><div className="page-title-group"><span className="eyebrow">성과 한눈에</span><h1>성과 통합 현황</h1><p>순매출, 유튜브→스토어 전환, 주간 KPI와 브랜드 관리자 연결을 한 기간으로 봅니다.</p></div><label className="month-control"><input type="month" value={month} onChange={(event) => setMonth(event.target.value)} /></label></header>{error ? <div className="inline-alert danger"><CircleAlert size={16} /> {error}</div> : null}<section className="metric-grid compact-metrics"><div className="metric-card"><div className="metric-top"><span>통합 순매출</span><CircleDollarSign size={16}/></div><div className="metric-value growth-money">{manwon(actual)}</div><div className="metric-caption">총매출 {manwon(gross)}</div></div><div className="metric-card"><div className="metric-top"><span>주문</span><BarChart3 size={16}/></div><div className="metric-value">{orders.toLocaleString("ko-KR")}</div><div className="metric-caption">브랜드 합산</div></div><div className="metric-card"><div className="metric-top"><span>조회→구매</span><Filter size={16}/></div><div className="metric-value">{averageConversion.toFixed(2)}%</div><div className="metric-caption">유튜브 유입 기준</div></div><div className="metric-card"><div className="metric-top"><span>주간 KPI</span><TrendingUp size={16}/></div><div className="metric-value">{kpis.length}</div><div className="metric-caption">최근 입력 지표</div></div></section><section className="growth-grid"><article className="panel brand-performance"><div className="panel-header"><div><h2>브랜드별 순매출</h2><p>{month.replace("-", "년 ")}월 · 만원</p></div><Link className="panel-link" href="/performance/revenue">매출 원장 <ArrowRight size={13}/></Link></div><div>{brandRows.map(row=><div key={row.name}><span><strong>{row.name}</strong><small>{manwon(row.amount)}</small></span><div><i style={{width:`${actual?Math.max(row.amount/actual*100,2):0}%`}}/></div><em>{actual?Math.round(row.amount/actual*100):0}%</em></div>)}{!brandRows.length?<div className="quiet-state"><CircleDollarSign/><strong>이번 달 매출 없음</strong></div>:null}</div></article><article className="panel"><div className="panel-header"><div><h2>주간 KPI 신호</h2><p>전주 대비 최근 입력</p></div><Link className="panel-link" href="/performance/weekly-kpi">KPI 입력 <ArrowRight size={13}/></Link></div><div className="funnel-signal-list">{kpis.map(item=>{const previous=Number(item.metadata.previousValue??0),current=Number(item.metric_current??0),change=previous?(current-previous)/Math.abs(previous)*100:0;return <div key={item.id}><span className={Math.abs(change)>=10?"bad":"good"}>{change>=0?"+":""}{change.toFixed(1)}%</span><span><strong>{item.title}</strong><small>{item.brand||"통합"} · {current}{item.metric_unit}</small></span></div>})}</div></article></section><section className="panel growth-links"><Link href="/performance/revenue"><CircleDollarSign size={16}/><span><strong>매출 원장</strong><small>총매출·취소·환불·순매출</small></span><ArrowRight size={14}/></Link><Link href="/performance/funnels"><Filter size={16}/><span><strong>유입 퍼널</strong><small>유튜브 조회에서 스토어 구매까지</small></span><ArrowRight size={14}/></Link><Link href="/performance/customers"><Link2 size={16}/><span><strong>브랜드 관리자</strong><small>{connections.length}개 연결 · 고객 원문은 외부 시스템에서 관리</small></span><ArrowRight size={14}/></Link></section></>;
}
